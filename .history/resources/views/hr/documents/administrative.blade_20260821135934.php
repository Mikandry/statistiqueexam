<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">

    <style>
        /* ============================================================
           MARGES ET FORMAT PAGE (A4)
           ============================================================ */
        @page {
            margin: 2.2cm 2.5cm 2.5cm 2.5cm;
            size: A4;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            font-size: 12pt;
            color: #1e293b;
            line-height: 1.6;
            background: white;
        }

        /* ============================================================
           EN-TÊTE INSTITUTIONNEL
           ============================================================ */
        .institution {
            text-align: center;
            font-weight: bold;
            font-size: 11pt;
            line-height: 1.4;
            letter-spacing: 0.3px;
            text-transform: uppercase;
        }

        .institution .stars {
            font-size: 10pt;
            letter-spacing: 2px;
            color: #0f766e;
            margin: 2px 0;
        }

        .header-rule {
            border-bottom: 2.5px solid #0f766e;
            width: 75%;
            margin: 12px auto 0;
        }

        /* ============================================================
           RÉFÉRENCE
           ============================================================ */
        .reference {
            text-align: right;
            font-size: 11pt;
            font-weight: 500;
            margin-top: 28px;
            color: #334155;
        }

        /* ============================================================
           TITRE PRINCIPAL
           ============================================================ */
        .title {
            text-align: center;
            font-size: 18pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #0f172a;
            margin: 40px 0 30px;
            padding-bottom: 8px;
            border-bottom: 1px solid #cbd5e1;
            display: inline-block;
            width: 100%;
        }

        /* ============================================================
           CORPS DU TEXTE
           ============================================================ */
        .body-text {
            text-align: justify;
            margin: 1.2em 0;
            font-size: 12pt;
        }

        /* ============================================================
           TABLEAU DES INFORMATIONS DE L'AGENT
           ============================================================ */
        .agent-info {
            margin: 20px 0 18px;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            background: #f8fafc;
            padding: 10px 14px;
        }

        .agent-info table {
            width: 100%;
            border-collapse: collapse;
            font-size: 11.5pt;
        }

        .agent-info td {
            vertical-align: top;
            padding: 5px 8px 5px 0;
        }

        .agent-info .label {
            font-weight: 600;
            color: #334155;
            width: 32%;
            white-space: nowrap;
        }

        .agent-info .label::after {
            content: " :";
        }

        .agent-info td:last-child {
            font-weight: 500;
            color: #0f172a;
        }

        /* ============================================================
           ENCADRÉS (ÉVÉNEMENTS)
           ============================================================ */
        .event-box {
            margin: 18px 0 22px;
            padding: 14px 18px;
            border-left: 4px solid #0f766e;
            background: #f1f5f9;
            border-radius: 4px;
            font-size: 11.5pt;
        }

        .event-box p {
            margin: 4px 0;
        }

        .event-box strong {
            color: #0f172a;
        }

        /* ============================================================
           SIGNATURE
           ============================================================ */
        .signature {
            margin-top: 70px;
            text-align: right;
            padding-right: 20px;
        }

        .signature .ville-date {
            font-weight: 500;
            color: #334155;
            margin-bottom: 30px;
        }

        .signature .fonction {
            font-weight: 600;
            font-size: 11.5pt;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #1e293b;
        }

        .signature .nom-signataire {
            font-weight: bold;
            font-size: 13pt;
            margin-top: 6px;
        }

        .signature .signature-line {
            width: 220px;
            border-bottom: 1.5px solid #1e293b;
            margin: 10px auto 0;
            display: inline-block;
        }

        /* ============================================================
           PIED DE PAGE (optionnel)
           ============================================================ */
        .footer {
            margin-top: 50px;
            font-size: 9pt;
            text-align: center;
            color: #94a3b8;
            border-top: 1px solid #e2e8f0;
            padding-top: 14px;
        }

        /* ============================================================
           SAUT DE PAGE
           ============================================================ */
        .page-break {
            page-break-before: always;
        }
    </style>
</head>

<body>
    <!-- ====== TOUT LE CODE PHP / HTML EXISTANT EST CONSERVÉ ====== -->
    <!-- (Le contenu reste strictement identique à l'original)      -->
</body>
</html>